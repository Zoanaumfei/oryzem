/* Base para todos os botões */
button {
    font-family: inherit;
    cursor: pointer;
    border: none;
    transition: all 0.2s;
}

/* Classes de botões */
.btn {
    padding: var(--spacing-sm) var(--spacing-lg);
    border-radius: var(--border-radius);
    font-size: var(--font-size-base);
}

.btn--primary {
    background-color: var(--primary-color);
    color: white;
}
/* ... resto do sistema de botões que mostrei antes */